package pulse_xp_system;

/**
 * The interface is aimed at modifying the Enter button action on each applied instance.
 */

public interface ActionForEnterButton {

    /**
     *
     * @param actionEvent; Allows in the event acted upon by the Enter button.
     */
    void actionOnEnterButton(javafx.event.ActionEvent actionEvent);
}
